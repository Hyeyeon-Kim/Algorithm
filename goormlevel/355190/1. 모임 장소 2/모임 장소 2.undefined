# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean

n = int(input())
arr = [[0] * n for _ in range(2)]

for i in range(n):
	arr[0][i], arr[1][i] = map(int, input().split())

arr[0].sort()
arr[1].sort()

if n % 2 == 0:
	idx = n//2 - 1
else:
	idx = n//2

print(arr[0][idx], arr[1][idx])