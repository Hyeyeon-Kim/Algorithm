# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
n = int(input())
arr = list(map(int, input().split()))
dp = [0] * n


for i in range(n):
	if i and arr[i] - arr[i-1] == 1:
		dp[i] = arr[i] + dp[i-1]
	else:
		dp[i] = arr[i]

print(max(dp))