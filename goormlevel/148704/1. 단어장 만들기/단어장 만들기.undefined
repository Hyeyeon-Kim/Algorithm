# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
n, k = map(int, input().split())
dic = []

for i in range(n):
	s = input()
	dic.append(s)

dic.sort(key = lambda x: (len(x), x))
print(dic[k-1])
